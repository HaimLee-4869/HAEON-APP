import type { ComponentType } from 'react';
import type { BottomTabBarProps } from '@react-navigation/bottom-tabs';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { ChartNoAxesCombined, House, ShoppingCart, Siren, Waves, type LucideProps } from 'lucide-react-native';
import { colors } from '@/constants/theme';
const config: Record<string, { label: string; icon: ComponentType<LucideProps> }> = { tide: { label: '물때', icon: Waves }, sos: { label: '신고', icon: Siren }, index: { label: '홈', icon: House }, report: { label: 'AI 리포트', icon: ChartNoAxesCombined }, store: { label: '스토어', icon: ShoppingCart } };
export function BottomTabBar({ state, descriptors, navigation, insets }: BottomTabBarProps) { return <View style={[styles.bar, { paddingBottom: Math.max(insets.bottom, 5), height: 69 + Math.max(insets.bottom, 5) }]}>{state.routes.map((route, index) => { const item = config[route.name]; if (!item) return null; const focused = state.index === index; const Icon = item.icon; const center = route.name === 'index'; return <Pressable key={route.key} accessibilityRole="button" accessibilityState={focused ? { selected: true } : {}} onPress={() => { const event = navigation.emit({ type: 'tabPress', target: route.key, canPreventDefault: true }); if (!focused && !event.defaultPrevented) navigation.navigate(route.name, route.params); }} style={styles.item}><View style={center ? styles.center : undefined}><Icon size={center ? 24 : 20} color={center ? colors.surface : focused ? colors.primary : '#7B8190'} strokeWidth={center ? 2.2 : 1.8} /></View><Text style={[styles.label, focused && styles.active, center && styles.centerLabel]}>{item.label}</Text></Pressable>; })}</View>; }
const styles = StyleSheet.create({ bar: { flexDirection: 'row', backgroundColor: colors.surface, borderTopWidth: 1, borderTopColor: colors.line }, item: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 5 }, label: { fontSize: 10, fontWeight: '700', color: '#7B8190' }, active: { color: colors.primary }, center: { width: 50, height: 50, borderRadius: 25, marginTop: -28, backgroundColor: colors.primary, borderWidth: 4, borderColor: colors.surface, alignItems: 'center', justifyContent: 'center', shadowColor: colors.primary, shadowOpacity: 0.28, shadowRadius: 10, shadowOffset: { width: 0, height: 7 }, elevation: 7 }, centerLabel: { marginTop: -3 } });
