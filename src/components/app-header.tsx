import { Pressable, StyleSheet, Text, View } from 'react-native';
import { router } from 'expo-router';
import { Settings, ShieldCheck } from 'lucide-react-native';
import { colors } from '@/constants/theme';
export function AppHeader() { return <View style={styles.header}><Pressable onPress={() => router.replace('/')} style={styles.brand}><View style={styles.logo}><ShieldCheck size={19} color="white" strokeWidth={2} /></View><Text style={styles.name}>HAEON SAFE</Text></Pressable><Pressable accessibilityLabel="설정" onPress={() => router.push('/settings')} style={styles.settings}><Settings size={19} color="#606779" /></Pressable></View>; }
const styles = StyleSheet.create({ header: { height: 56, backgroundColor: colors.surface, borderBottomWidth: 1, borderBottomColor: colors.line, paddingHorizontal: 16, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, brand: { flexDirection: 'row', alignItems: 'center', gap: 8 }, logo: { width: 30, height: 30, borderRadius: 9, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center', shadowColor: colors.primary, shadowOpacity: 0.22, shadowRadius: 8, shadowOffset: { width: 0, height: 4 }, elevation: 3 }, name: { fontSize: 13, fontWeight: '900', letterSpacing: -0.25, color: colors.text }, settings: { width: 36, height: 36, borderRadius: 18, backgroundColor: '#F7F5FC', alignItems: 'center', justifyContent: 'center' } });
